# Copy to .env (gitignored) for local `forge sync` runs against a real Jira
# site. See docs/testing.md for how to get a free API token. Never commit
# real values, and never wire these into CI.

JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=
